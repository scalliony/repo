{
    "private": true,
    "workspaces": [
        "*"
    ],
    "scripts": {
        "build": "npm run --ws build",
        "build:dev": "npm run --ws build:dev",
        "prestart": "npm run build",
        "start": "cargo run -q --release",
        "prestart:dev": "npm run build:dev",
        "start:dev": "cargo run -q",
        "test": "npm run --ws test"
    }
}
